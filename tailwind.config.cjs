module.exports = {
  // Tailwind configuration removed – all custom styles are defined in CSS.

  // The new Astro version does not require a multiple variant config in Tailwind.
  // If you need to enable dark mode via class strategy, you can set it in your CSS using the [data-theme] attribute.
  // This file will be ignored by the Tailwind compiler.
};
